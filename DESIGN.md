# DeskPilot 设计文档

> 本文档描述当前实现和演进约定。具体功能状态以 README 的“当前功能”和代码为准。

## 1. 设计目标

DeskPilot 是一个本地优先的开发者知识管理工作台，用于保存和整理笔记、项目文档以及代码片段。

设计重点：

- 数据默认保存在本机，断网时仍可使用
- 界面保持轻量，减少信息管理本身带来的负担
- 主进程负责系统能力和数据访问，渲染进程只负责界面
- 为全文检索、文件导入和备份能力预留清晰的扩展边界

## 2. 产品边界

### 当前范围

- 创建、查看和编辑笔记
- 按分类组织笔记
- 按标题、分类和正文进行全文检索
- 使用 SQLite 保存笔记

### 暂不处理

- 多设备实时协作
- 云端账号体系
- 复杂的团队权限
- 自动同步外部服务

这些能力会引入账号、冲突解决和数据安全等额外问题，暂不放入本地单机版本。

## 3. 系统架构

```text
┌─────────────────────────────────────────────┐
│                 Renderer                     │
│  笔记列表 · 搜索 · 编辑器 · UI 状态           │
└──────────────────┬──────────────────────────┘
                   │ window.deskPilot.notes
                   │ contextBridge / IPC
┌──────────────────▼──────────────────────────┐
│                  Preload                     │
│       暴露白名单 API，不暴露 Node.js          │
└──────────────────┬──────────────────────────┘
                   │ ipcRenderer.invoke
┌──────────────────▼──────────────────────────┐
│                 Main Process                 │
│       窗口管理 · IPC handlers · 数据库         │
└──────────────────┬──────────────────────────┘
                   │ better-sqlite3
┌──────────────────▼──────────────────────────┐
│                  SQLite                      │
│          用户数据目录 / deskpilot.db          │
└─────────────────────────────────────────────┘
```

### 进程职责

#### Renderer

负责页面渲染、用户交互和临时 UI 状态。不能直接访问文件系统、数据库或 Electron 的 Node.js API。

#### Preload

通过 `contextBridge` 暴露最小化的、按领域划分的 API。所有传入主进程的数据都应在主进程重新校验。

#### Main Process

负责创建窗口、初始化数据库、处理 IPC 请求以及未来的文件读写、系统托盘和全局快捷键。

## 4. 数据模型

当前使用 `notes` 表：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | INTEGER | 自增主键 |
| `title` | TEXT | 笔记标题 |
| `category` | TEXT | 所属分类 |
| `content` | TEXT | Markdown 原文 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 最后更新时间 |
| `deleted_at` | TEXT | 软删除时间，`NULL` 表示正常笔记 |
| `source_type` | TEXT | 来源类型，如 `file` 或 `url` |
| `source_ref` | TEXT | 来源文件路径或规范化 URL |

数据库启用 WAL 模式，以改善读取期间的写入体验。初始化逻辑使用 `CREATE TABLE IF NOT EXISTS`，并通过补充字段迁移兼容已有数据库。

另有 `notes_fts` FTS5 外部内容表，索引 `title`、`category` 和 `content`；`notes_ai`、`notes_au`、`notes_ad` 触发器负责同步索引。

## 5. IPC API

渲染进程只能通过 preload 提供的接口访问笔记：

| API | 用途 | 返回值 |
| --- | --- | --- |
| `notes.list()` | 获取按更新时间排序的笔记 | 笔记数组 |
| `notes.create(note)` | 创建一条笔记 | 新建笔记 |
| `notes.update(note)` | 更新标题、分类和内容 | 更新后的笔记 |

当前已实现的扩展 API：

| API | 用途 |
| --- | --- |
| `notes.listDeleted()` | 获取回收站笔记 |
| `notes.search(query)` | 搜索标题、分类和正文 |
| `notes.delete(id)` | 软删除笔记 |
| `notes.restore(id)` | 恢复笔记 |
| `notes.purge(id)` | 永久删除回收站笔记 |
| `notes.importFile()` | 通过主进程选择并导入文本文件 |
| `notes.importUrl(url)` | 下载并导入网页文本 |
| `notes.exportBackup()` | 导出 JSON 备份 |
| `notes.restoreBackup()` | 合并恢复 JSON 备份 |

IPC channel 使用领域命名，例如 `notes:list`、`notes:create` 和 `notes:update`。不提供通用的“执行任意 SQL”或“调用任意 IPC channel”接口。

## 6. 关键流程

### 应用启动

1. Electron 完成初始化
2. 在用户数据目录打开 `deskpilot.db`
3. 创建缺失的数据表
4. 空数据库写入示例笔记
5. 创建 BrowserWindow 并加载渲染页面
6. 渲染进程通过 `notes.list()` 获取数据

### 编辑保存

1. 用户修改编辑器内容
2. 更新渲染进程中的当前笔记
3. 等待短暂防抖时间，避免每次输入都写数据库
4. 调用 `notes.update()` 发送结构化数据
5. 主进程执行参数化 SQL
6. 保存成功后更新 UI 状态

### 全文检索

1. 渲染进程对输入进行短暂防抖
2. 通过 `notes.search(query)` 调用主进程
3. 主进程使用参数化 FTS5 `MATCH` 查询标题、分类和正文
4. 查询只返回未删除笔记

### 回收站

删除只写入 `deleted_at`，不会立即移除数据；恢复清空该字段，彻底删除才执行 SQL `DELETE`。

## 7. 安全约束

- 保持 `contextIsolation` 开启
- 不开启 `nodeIntegration`
- 只通过 preload 暴露明确的业务 API
- 数据库操作使用参数化查询
- 不允许渲染进程传递任意 SQL
- 页面设置基础 Content Security Policy
- 文件导入功能上线后，需要限制可读取的文件类型和路径
- 网页导入仅允许 HTTP/HTTPS，限制响应类型、大小和超时，并拒绝常见本地/内网地址
- 备份恢复必须校验格式、版本、大小和字段类型

## 8. 目录约定

```text
src/
├── main.js       # 主进程和数据库初始化
├── preload.js    # IPC API 白名单
├── renderer.js   # 当前界面和交互
└── index.css     # 全局样式
test/
└── database.test.js # FTS5、回收站和恢复行为测试
```

随着功能增加，建议按领域拆分：

```text
src/
├── main/
│   ├── database.js
│   └── ipc/
├── renderer/
│   ├── components/
│   ├── stores/
│   └── views/
└── shared/
```

## 9. 演进计划

### 阶段一：编辑体验

- Markdown 编辑与预览
- 标题、标签和分类编辑
- 保存失败提示和重试
- 笔记删除与恢复

阶段一实现约定：删除采用软删除（`deleted_at`），普通列表不展示已删除笔记；回收站支持恢复和彻底删除，相关操作通过独立的 IPC API 提供。

### 阶段二：内容获取

- SQLite FTS5 全文检索（已完成）
- 文件导入和基础文本提取（已完成）
- 网页导入和基础 HTML 清理（已完成）
- 数据导出与备份恢复（已完成）
- 拖拽导入（已完成）
- 来源元数据和去重（已完成）

阶段二约定：文件导入由主进程弹出选择器，仅接受指定文本类型且单文件上限 5 MB；网页导入仅允许 HTTP/HTTPS，限制响应类型、大小和超时，并移除脚本和样式；备份使用 JSON，恢复采用合并策略并生成新 ID。

### 阶段三：暂不规划

AI 总结、问答和模型接入暂不纳入产品范围，后续如重新规划，需要单独评估隐私、模型配置和上下文边界。

### 阶段四：可维护性

- 将 UI 拆分为组件
- 增加单元测试和端到端测试（数据层基础测试已完成）
- 数据库迁移版本管理
- 跨平台打包和自动更新

## 10. 设计决策记录

### 为什么选择 SQLite

笔记数据具有结构化特征，需要可靠的查询、事务和备份能力。SQLite 不需要单独运行服务，适合本地桌面应用。

### 为什么不让 Renderer 直接访问数据库

这样会扩大渲染层的权限边界，也会让数据访问逻辑和界面耦合。通过 preload 和 IPC 集中管理数据访问，更容易进行校验、迁移和测试。

### 为什么先保存 Markdown 原文

Markdown 是可读、可导出、容易迁移的格式。编辑器和预览器可以替换，而不需要改变底层数据模型。
